apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: grafana
  name: grafana-service
  namespace: monitoring
spec:
  ports:
  - nodePort: 31330
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    k8s-app: grafana
  type: NodePort
